#include "firstWindow.h"

firstWindow::firstWindow(unsigned int height, unsigned int width)
	: Windows(height, width)
{}
//-------------------------------------------------------------------
void firstWindow::readFromFile()
{
	std::cout << "reading from file .../n";
}
//-------------------------------------------------------------------
void firstWindow::customerChoice()
{
	// firs the function will open the window
	// and after this the function need to check in which button the user clicked
}
